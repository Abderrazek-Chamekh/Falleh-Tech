<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>

<BorderPane fx:id="border" prefWidth="800.0" prefHeight="500.0" xmlns:fx="http://javafx.com/fxml"
            stylesheets="@loginstyle.css" fx:controller="tn.esprit.Controllers.User.Authentication.LoginController">

    <left>
        <AnchorPane fx:id="leftPane" prefWidth="400.0" styleClass="left-pane">
            <ImageView layoutX="50" layoutY="70" fitWidth="300" fitHeight="360" pickOnBounds="true" preserveRatio="true">
                <image>
                    <Image url="@/Images/farmer.png" />
                </image>
            </ImageView>
        </AnchorPane>
    </left>

    <center>
        <AnchorPane>
            <VBox fx:id="loginCard" alignment="CENTER" spacing="20" styleClass="login-card" prefWidth="400" layoutX="0" layoutY="50" AnchorPane.leftAnchor="0.0" AnchorPane.rightAnchor="0.0">
                <Label text="Bienvenue, Agriculteur 🌱" styleClass="title" />

                <!-- Add the error label here -->
                <Label fx:id="errorLabel" styleClass="error-label" visible="false" />

                <TextField fx:id="emailField" promptText="Email" styleClass="input-field"/>
                <PasswordField fx:id="passwordField" promptText="Mot de passe" styleClass="input-field"/>

                <Button fx:id="loginButton" text="🌾 Se Connecter" styleClass="green-button"/>
                <Button fx:id="googleButton" text="G Connexion Google" styleClass="google-button"/>

                <HBox alignment="CENTER" spacing="5">
                    <Label text="Pas encore inscrit ?" />
                    <Hyperlink fx:id="signup" text="Rejoignez-nous !" />
                </HBox>

                <Hyperlink text="Mot de passe oublié ?" styleClass="forgot-password" />
            </VBox>
        </AnchorPane>
    </center>
</BorderPane>